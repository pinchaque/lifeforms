# returns a random point the specified distance away from x, y
# note that you will need to check this returned point against your bounds
# since this function doesn't know about those
def rnd_point(x, y, dist)
  # TODO: Implement
  return x + dist, y
end